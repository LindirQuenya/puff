[package]
name = "enerdhil"
version = "0.1.0"
edition = "2021"

[dependencies]
num = "0.4.3"
thiserror = "1.0.61"
serde = { version = "1.0", optional = true, features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.117"
num = {version = "0.4.3", features = ["serde"]}

[features]
serde = ["dep:serde"]